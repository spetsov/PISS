<div class="navbar-header">
    <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
    </button>
</div>
<div class="collapse navbar-collapse">
    <ul class="nav navbar-nav">
        <li>@Html.ActionLink("Home", "Index", "Home")</li>
        @if (Roles.IsUserInRole("Admin"))
        {
            <li>@Html.ActionLink("Administration", "Index", "Administration")</li>
        }
        @if (Roles.IsUserInRole("Student"))
        {
            <li>@Html.ActionLink("My Assignment", "Index", "Students")</li>
            <li>@Html.ActionLink("My Thesis", "Thesis", "Students")</li>
            <li>@Html.ActionLink("My Status", "DiplomaStatus", "Students")</li>
            <li>@Html.ActionLink("Work Experience", "WorkExperience", "Students")</li>
        }
        @if (Roles.IsUserInRole("Doctorant"))
        {
            <li>@Html.ActionLink("Work Plan", "Index", "Doctorant")</li>
        }
        @if (Roles.IsUserInRole("Teacher"))
        {
            <li>@Html.ActionLink("Students diplomas", "Index", "Teacher")</li>
            <li>@Html.ActionLink("Doctorants doctorates", "Doctorants", "Teacher")</li>
            <li>@Html.ActionLink("Work Experience Requests", "AllWorkExperience", "Teacher")</li>
        }
    </ul>
     <div class="login-partial">
                @Html.Partial("_LoginPartial")
     </div>
</div>
